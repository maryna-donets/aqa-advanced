//4.1
const radius=4;
const square=Math.PI*Math.pow(radius, 2);
console.log(square.toFixed(2));

//4.2
const length=3.75;
const width=2.5;
const square1=length*width;
console.log(square1.toFixed(2));

//4.3
const radius1=0.3;
const height=0.8;
const volume=Math.PI*Math.pow(radius,2)*height;
console.log(volume.toFixed(2));